<?php
declare(strict_types=1);

use Symfony\Component\DependencyInjection\ContainerBuilder;

return static function (ContainerBuilder $container, array $config): void {
    // Добавьте здесь регистрацию своих сервисов или переопределите базовые.
    // Пример: $container->setDefinition('app.custom_service', new Definition(...));
};
