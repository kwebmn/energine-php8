/**
 * Legacy stub for jsTree.
 *
 * The actual jsTree library ships inside the Vite vendor bundle, which
 * imports the Composer-provided package `components/jstree`.
 * This placeholder keeps historical component configurations working in
 * debug mode without committing the full upstream distribution into the
 * repository.
 */
(function () {
    if (typeof window === 'undefined') {
        return;
    }

    var jqueryInstance = window.jQuery || window.$;
    if (!jqueryInstance || !jqueryInstance.fn) {
        if (typeof console !== 'undefined' && typeof console.warn === 'function') {
            console.warn('jsTree vendor bundle was not loaded before the legacy stub.');
        }
        return;
    }

    if (!jqueryInstance.fn.jstree || !jqueryInstance.jstree) {
        if (typeof console !== 'undefined' && typeof console.warn === 'function') {
            console.warn('jsTree plugin was not initialised on the global jQuery instance.');
        }
    }
}());
